# ================================================================
# シークレット設定（.envファイル）
# ================================================================
# このファイルには機密情報（シークレット）のみを設定してください
# その他の設定は config.yaml で行います

# Discord Botトークン（必須）
# Discord Developer Portalで取得: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Anthropic APIキー（必須）
# Anthropic Consoleで取得: https://console.anthropic.com/
# または Z.AIコンソールで取得
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Anthropic Base URL（Z.AI使用時に必須）
# config.yaml の anthropic.base_url と同じ値を設定
ANTHROPIC_BASE_URL=https://api.z.ai/api/anthropic

# エージェントワークスペースのルートディレクトリ（オプション）
# 設定しない場合は、各エージェントのディレクトリ配下にworkspaceが作成されます
# この設定は、agent.yamlで個別に指定されていないエージェントに適用されます
# 例: /data/agent_workspaces
# AGENT_WORKSPACE_ROOT=/path/to/your/workspace

# 個別エージェントのワークスペース設定は agent.yaml で行います:
# agents/your-agent/agent.yaml:
#   workspace: /path/to/specific/workspace
